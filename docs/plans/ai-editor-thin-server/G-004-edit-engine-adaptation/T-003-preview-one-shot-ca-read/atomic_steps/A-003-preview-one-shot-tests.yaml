step_id: A-003
parent_tactical_step: T-003
name: Preview one-shot unit tests
target_file: tests/unit/test_universal_file_preview_oneshot.py
operation: create_file
priority: 1
depends_on:
- A-001
concepts:
- C-011
- C-023
prompt: |
  Create **`tests/unit/test_universal_file_preview_oneshot.py` only** (operation: create_file).
  Unit tests for one-shot `universal_file_preview` runtime branch (C-011, C-023).
  Do **not** modify runtime, metadata, facade, or other test files in this step.

  **Prerequisite:** The one-shot runtime branch in `universal_file_preview_runtime.py` must
  already be implemented on disk before creating this test file.

  ## MRS concepts (relevant excerpt)

  - **C-011 Edit Stage**: one-shot preview is read-only; no File Subtree created.
  - **C-023 CA API Contract**: one-shot uses `download_without_lock`; no `session_open_file`.

  ## File to create — full content

  Create the file with exactly this content (adjust import path for `_run_preview_on_abs_path`
  only if the helper name or module differs from the baseline below once the one-shot branch
  in `universal_file_preview_runtime.py` is on disk; default patch target is
  `ai_editor.commands.universal_file_preview_runtime._run_preview_on_abs_path`):

  ```python
  """Unit tests for one-shot universal_file_preview (C-011, C-023)."""
  from __future__ import annotations

  from pathlib import Path
  from unittest.mock import MagicMock, patch

  import pytest

  from mcp_proxy_adapter.commands.result import ErrorResult, SuccessResult


  @pytest.fixture(autouse=True)
  def _clear_bundle_index() -> None:
      import ai_editor.commands.universal_file_edit.session as session_mod

      session_mod._session_bundles.clear()
      session_mod._file_open_index.clear()


  def test_oneshot_download_without_lock_no_bundle() -> None:
      """One-shot path calls download_without_lock when file not in bundle index."""
      from ai_editor.commands.universal_file_preview_runtime import run_preview_execute

      command = MagicMock()
      command._resolve_project_root = MagicMock(return_value=Path("/tmp/proj"))
      mock_client = MagicMock()
      mock_client.download_without_lock.return_value = b"print('hi')\n"
      with (
          patch(
              "ai_editor.commands.universal_file_preview_runtime.get_code_analysis_client",
              return_value=mock_client,
          ),
          patch(
              "ai_editor.commands.universal_file_preview_runtime._run_preview_on_abs_path",
              return_value=SuccessResult(data={"session_origin": "none", "focus": {}}),
          ) as mock_preview,
      ):
          result = run_preview_execute(
              command,
              project_id="p1",
              file_path="a.py",
          )
      assert isinstance(result, SuccessResult)
      mock_client.download_without_lock.assert_called_once_with(
          project_id="p1", file_path="a.py"
      )
      mock_client.session_open_file.assert_not_called()
      mock_preview.assert_called_once()


  def test_open_file_rejects_oneshot() -> None:
      """Open bundle present → OPEN_FILE_USE_WORKSPACE_PREVIEW, no download."""
      import ai_editor.commands.universal_file_edit.session as session_mod
      from ai_editor.commands.universal_file_preview_runtime import run_preview_execute

      session_mod._file_open_index[("p1", "a.py")] = "ca-sid-1"
      command = MagicMock()
      mock_client = MagicMock()
      with patch(
          "ai_editor.commands.universal_file_preview_runtime.get_code_analysis_client",
          return_value=mock_client,
      ):
          result = run_preview_execute(
              command,
              project_id="p1",
              file_path="a.py",
          )
      assert isinstance(result, ErrorResult)
      assert result.code == "OPEN_FILE_USE_WORKSPACE_PREVIEW"
      mock_client.download_without_lock.assert_not_called()
  ```

  ## Test expectations

  - `test_oneshot_download_without_lock_no_bundle`: asserts `download_without_lock` called once
    with `project_id="p1"`, `file_path="a.py"`; `session_open_file` never called; preview helper
    invoked once; result is `SuccessResult`.
  - `test_open_file_rejects_oneshot`: with `("p1", "a.py")` in `_file_open_index` and no
    `session_id`, returns `ErrorResult` with code `OPEN_FILE_USE_WORKSPACE_PREVIEW`;
    `download_without_lock` not called.

  ## Forbidden

  - Do **not** modify any file other than `tests/unit/test_universal_file_preview_oneshot.py`.
  - Do **not** call live upstream services; mock `get_code_analysis_client` and preview helper.
  - Do **not** add tests for open-file workspace preview (that is T-002 scope).

  ## Validation (mandatory after implementation)

  ```
  source .venv/bin/activate
  black tests/unit/test_universal_file_preview_oneshot.py
  flake8 tests/unit/test_universal_file_preview_oneshot.py
  mypy tests/unit/test_universal_file_preview_oneshot.py
  pytest tests/unit/test_universal_file_preview_oneshot.py -v
  ```

  Expected:
  - black: "reformatted" or "All done!"
  - flake8: no output (exit 0)
  - mypy: "Success: no issues found" on touched file
  - pytest: all PASSED; one-shot uses download_without_lock; open-file returns OPEN_FILE_USE_WORKSPACE_PREVIEW

  **All tests must pass.**
verification:
  type: pytest
  target: tests/unit/test_universal_file_preview_oneshot.py
  expected: >
    All tests PASSED. One-shot preview downloads via download_without_lock without
    session_open_file; when file is open in bundle index without session_id, returns
    OPEN_FILE_USE_WORKSPACE_PREVIEW.
status: draft
