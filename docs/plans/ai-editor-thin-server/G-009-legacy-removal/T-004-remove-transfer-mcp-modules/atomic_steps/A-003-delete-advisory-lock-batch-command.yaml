step_id: A-003
parent_tactical_step: T-004
name: Удалить advisory lock batch модуль
target_file: ai_editor/commands/project_file_advisory_lock_batch_command.py
operation: delete_file
priority: 3
depends_on: []
concepts:
- C-017
- C-020
prompt: |
  Precondition: execute ONLY after BOTH of the following are complete:
  - G-002/T-005 (upload transfer wrapper fully implemented in Upstream Client, C-017)
  - G-006/T-002 (transfer/lock MCP commands unregistered from hooks_register_part2;
    ProjectFileAdvisoryLockBatchCommand and related advisory-lock classes are no longer
    registered on the MCP surface — unregistration is owned by G-006; do NOT unregister here)

  Delete `ai_editor/commands/project_file_advisory_lock_batch_command.py`.

  This file implements the legacy public MCP advisory-lock command (C-020):
  ProjectFileAdvisoryLockBatchCommand (MCP command name: project_file_advisory_lock_batch).
  Lock/unlock for thin workflow is handled via Upstream Client (C-017) calling CA API
  session_open_file / session_close_file. After G-006/T-002 this class is no longer
  registered as an MCP command; this step removes the module from disk only.

  Do not modify any other file in this step.
  Do not modify hooks.py — removing the register_auto_import_module line for
  project_file_advisory_lock_batch_command from hooks.py is out of scope for this
  delete_file step.
  Do not modify registration.py or perform any MCP unregistration in this step.
  project_file_lock_status_command.py is a separate file and is not in scope of this step.
verification:
  type: static_analysis
  target: ai_editor/commands/project_file_advisory_lock_batch_command.py
  expected: >
    File absent from repository; no MCP registration exists for
    ProjectFileAdvisoryLockBatchCommand (MCP surface removal verified via
    G-006/T-002 precondition, not re-checked in this step).
status: draft
